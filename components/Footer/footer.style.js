import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        marginTop: 30,
        backgroundColor: '#fff',
        height: 25,
        borderRadius: 20,
    },
    containerTxt: {
        color: '#757575',
        fontWeight: 700,
        marginTop: 4,
        marginLeft: 45,
    },
})

export default styles;